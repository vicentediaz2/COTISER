import type { EditableQuotation } from "./QuotationForm";

export type HtmlOrganization = { nombre: string; direccion?: string | null; eslogan?: string | null; telefono?: string | null; correo?: string | null; direccion_web?: string | null; logo?: string | null };
export type HtmlQuotation = EditableQuotation & { status: string; total: number; subtotal: number; taxRate?: number | null; date: string };
const labels: Record<string, string> = { pendiente: "Pendiente", enviada: "Enviada", aprobada: "Aprobada", rechazada: "Rechazada", vencida: "Vencida" };
const money = new Intl.NumberFormat("es-CL", { style: "currency", currency: "CLP", maximumFractionDigits: 0 });
const value = (text?: string | null) => text?.trim() || "—";

export function QuotationHtmlDocument({ quotation, organization, fullPage = false }: { quotation: HtmlQuotation; organization: HtmlOrganization | null; fullPage?: boolean }) {
  const discountAmount = Math.round(quotation.subtotal * (quotation.discount ?? 0) / 100);
  const taxAmount = Math.round((quotation.subtotal - discountAmount) * (quotation.taxRate ?? 0) / 100);
  const shellClass = fullPage ? "min-h-screen w-screen bg-white text-slate-950" : "min-h-screen bg-slate-100 p-4 text-slate-950 sm:p-8";
  const articleClass = fullPage ? "min-h-screen w-full max-w-none bg-white p-7 shadow-none sm:p-12" : "mx-auto min-h-[calc(100vh-4rem)] max-w-6xl bg-white p-7 shadow-sm sm:p-12";
  return <main className={shellClass}><article className={articleClass}><header className="flex flex-col justify-between gap-6 border-b-2 border-blue-700 pb-7 sm:flex-row"><div className="flex items-start gap-4">{organization?.logo && <img src={organization.logo} alt="Logo" className="h-16 w-16 rounded-lg object-contain" />}<div><h1 className="text-2xl font-bold text-blue-800">{value(organization?.nombre)}</h1><p className="mt-1 text-sm text-slate-500">{value(organization?.eslogan)}</p><p className="mt-3 text-sm text-slate-600">{value(organization?.direccion)}</p><p className="text-sm text-slate-600">{value(organization?.telefono)} · {value(organization?.correo)}</p></div></div><div className="text-left sm:text-right"><p className="text-sm font-semibold uppercase tracking-widest text-blue-700">Cotización</p><p className="mt-2 text-2xl font-bold">#{quotation.id.slice(0, 8).toUpperCase()}</p><p className="mt-1 text-sm text-slate-600">{new Date(quotation.date).toLocaleDateString("es-CL")}</p><p className="mt-2 text-sm font-medium text-slate-600">Creada exclusivamente para {quotation.clientName || "el cliente"} el {new Date().toLocaleDateString("es-CL")}.</p><p className="mt-2 text-sm font-semibold text-slate-700">{labels[quotation.status] ?? quotation.status}</p></div></header><section className="grid gap-5 border-b border-slate-200 py-7 sm:grid-cols-2"><div><p className="text-xs font-semibold uppercase tracking-widest text-slate-500">Cliente</p><h2 className="mt-2 text-lg font-semibold">{value(quotation.clientName)}</h2><p className="mt-1 text-sm text-slate-600">{value(quotation.clientAddress)}</p><p className="text-sm text-slate-600">RUT / ID: {value(quotation.clientTaxId)}</p></div><div className="sm:text-right"><p className="text-xs font-semibold uppercase tracking-widest text-slate-500">Datos de contacto</p><p className="mt-2 text-sm text-slate-600">{value(organization?.correo)}</p><p className="text-sm text-slate-600">{value(organization?.telefono)}</p><p className="text-sm text-slate-600">{value(organization?.direccion_web)}</p></div></section><section className="py-7"><table className="w-full text-left text-sm"><thead><tr className="border-b-2 border-slate-200 text-xs uppercase tracking-wide text-slate-500"><th className="pb-3">Servicio</th><th className="pb-3 text-right">Cantidad</th><th className="pb-3 text-right">Valor unitario</th><th className="pb-3 text-right">Total</th></tr></thead><tbody>{quotation.items.map((item, index) => <tr key={`${item.description}-${index}`} className="border-b border-slate-100"><td className="py-4 font-medium">{item.description}</td><td className="py-4 text-right">{item.quantity}</td><td className="py-4 text-right">{money.format(item.unitPrice)}</td><td className="py-4 text-right font-semibold">{money.format(Math.round(item.quantity * item.unitPrice * (1 - (item.discount ?? 0) / 100)))}</td></tr>)}</tbody></table></section>{quotation.notes && <section className="border-t border-slate-200 py-5"><p className="text-xs font-semibold uppercase tracking-widest text-slate-500">Observaciones</p><p className="mt-2 whitespace-pre-wrap text-sm text-slate-700">{quotation.notes}</p></section>}<section className="ml-auto max-w-xs border-t-2 border-blue-700 pt-4"><div className="flex justify-between py-1 text-sm"><span>Subtotal</span><strong>{money.format(quotation.subtotal)}</strong></div>{discountAmount > 0 && <div className="flex justify-between py-1 text-sm"><span>Descuento</span><strong>-{money.format(discountAmount)}</strong></div>}{taxAmount > 0 && <div className="flex justify-between py-1 text-sm"><span>Impuesto ({quotation.taxRate}%)</span><strong>{money.format(taxAmount)}</strong></div>}<div className="mt-2 flex justify-between border-t border-slate-200 pt-3 text-lg"><span className="font-bold">Total</span><strong className="text-blue-700">{money.format(quotation.total)}</strong></div></section><footer className="mt-12 border-t border-slate-200 pt-5 text-center text-xs text-slate-500"><p>Cotización válida solo por 20 días.</p><p className="mx-auto mt-3 max-w-2xl leading-relaxed">Documento generado mediante CotizaPro, una herramienta de gestión y creación de cotizaciones. CotizaPro no participa ni se responsabiliza por los acuerdos, precios, servicios, pagos o compromisos establecidos entre las partes.</p></footer></article></main>;
}
